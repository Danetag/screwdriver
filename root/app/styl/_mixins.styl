ffregular() {
  font-family: 'Helvetica', Arial, sans-serif;
  font-weight: 400;
}

ffbold() {
  font-family: 'Helvetica', Arial, sans-serif;
  font-weight: 700;
}

em(target) {
  font-size : target / base-font-size + 0em;
}